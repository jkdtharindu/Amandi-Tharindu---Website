node_modules/
.DS_Store

# env — .env.example is intentionally tracked, so do NOT broaden this to .env*
.env
.env.local

# `vercel env pull` without -o writes an env file literally named "production"
# (or "preview"/"development") in the repo root, containing real secrets.
/production
/preview
/development

# next.js
.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# vercel
.vercel

# supabase local state (CLI-managed)
supabase/.branches
supabase/.temp

# legacy prototype build output
dist/

# database backups — these hold every guest's name, phone number and email.
# Never commit one; keep them off the repo and out of shared folders.
backups/
.env*
