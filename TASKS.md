# Project Tasks

This file tracks the implementation plan for the Amandi & Tharindu wedding website.

✅ **BRANCH DIVERGENCE RESOLVED (2026-09-04):** Merged `feature/ui-wrapping` into `feature/nextjs-supabase-migration` (commit 4a9f07a). Brought Theme Editor (P1-10), Section Manager (P1-11), Table Arrangement (P1-14), Supabase Storage adapter, and migrations 004–008. Database already had these applied; re-running them is idempotent. See BRANCH_STRATEGY.md for SOP to prevent this in future.

✅ **PR #4 MERGED TO MAIN (2026-09-04):** `feature/nextjs-supabase-migration` merged into `main` (merge commit 3560128), after resolving a second parallel divergence — `main` had independently built its own competing Theme Editor (see MEMORY.md 2026-09-04 "Past mistakes" for the reconciliation). All work described below — Next.js migration, admin panel, guest RSVP/seating, theme system — is now on `main`. The feature branch has been deleted, both locally and on origin. All work going forward happens directly on `main` until the next feature branch is opened (see BRANCH_STRATEGY.md).

## Project Status
- Status: Next.js 16 migration complete; admin panel (auth, guest CRUD, RSVP dashboard) built and verified; WhatsApp reminder slim slice added; theme editor (P1-10), section manager (P1-11), table planning/seating (P1-14), and event manager (P1-09, MVP scope) all built, merged, and live in the Next.js app (`app/admin/`). P1-11/P1-14 were backend-only until 2026-09-04 — a docs-accuracy audit found both marked done despite no Next.js admin UI existing; the UI wiring and event manager both landed the same day.
- Current focus (updated 2026-09-10, deployment session): **the site is live — https://amandi-tharindu-website.vercel.app (Action 10, done 2026-09-10).** The pre-login site gate (P1-15, Action 12) is merged (PR #11) and confirmed working on the live deploy. Backups (Action 14) are done — a real restore succeeded 2026-09-10. **Top remaining item: Action 18a**, the deferred Neon password reset — the owner chose to deploy before resetting it, so the exposed password is now live in Vercel's `DATABASE_URL`, which is the one open item with real (if still low-probability) exposure. Also still open: the three owner-credential checks in Action 10 (guest login/RSVP, admin login, WhatsApp link), and adding `[Code]` to the `reminder_2` template before sending it. Earlier context: Security work is **closed** — Action 8 was right-sized to Tier 1 on 2026-09-06, and PR #7 (the remaining security branch) was closed unmerged on 2026-09-09 as superseded by main's own rate limiter (see MEMORY.md 2026-09-09). Mobile review shipped 2026-09-06; a follow-on cascade-layer fix for invisible dark-link text merged 2026-09-09 (PR #8). Deployment runbook merged 2026-09-09 (PR #9, Action 10). P1 remaining: the pre-login site gate (P1-15, still blocked on the same product questions — see Next Action 12). **2026-09-10** was a short docs-accuracy and tooling session — no feature work: corrected Action 8's image-validation claim (see MEMORY.md 2026-09-10), deleted the merged `focused-kilby-265cc0` worktree, which took `npm run lint` from 5,298 problems to 4, and rescued an uncommitted `Countdown.tsx` hooks fix out of that worktree onto `main` before deleting it.
- Priority (updated 2026-09-10, deployment session): (1) **Action 18a** — reset the Neon `neondb_owner` password (deferred by owner decision before the deploy, now the one item with live exposure) and update `DATABASE_URL` in `.env` and Vercel; (2) the three owner-credential checks from Action 10's §5 (guest login/RSVP, admin login, WhatsApp link); (3) add `[Code]` to the `reminder_2` WhatsApp template before sending it (PRD §15). ~~Owner merge decision on the site gate~~ — merged 2026-09-10 (PR #11). ~~The backup restore rehearsal (Action 14)~~ — done 2026-09-10. ~~Deployment (Action 10)~~ — done 2026-09-10, live at https://amandi-tharindu-website.vercel.app.
- **2026-09-11 session:** (1) Found and fixed a real bug the owner reported ("changes we apply don't reflect on the guest panels") — Home/Our Story/Celebration/Gallery/Wishes are all statically prerendered at build time with no revalidation, so admin edits to theme/sections/events never reached guests until the next `vercel deploy`. Fixed by calling `revalidatePath` from every theme/section/event save/create/delete route (new `src/revalidatePublicPages.js`), verified end-to-end against a local production build (not just dev, which never shows this bug). Committed (`8ccfcce`), not yet deployed. Caveat found during verification: this only worked locally when built with `next build --webpack`; the default Turbopack build silently did not pick up the on-demand revalidation. Vercel's hosting has its own separate cache mechanism so this is expected to work regardless, but **has not been confirmed against the real Vercel deployment** — worth one quick check after the next deploy (edit a theme color, reload a guest page once, confirm it updates). (2) Built image uploads for Sections and Event venue photos (Next Action 20 below) — Vercel Blob storage, owner's choice over Supabase Storage (never actually set up for this project) or a paste-a-link workaround.

## Working Principles
- Use strict TDD for each slice
- Each slice must be end-to-end and independently testable
- Prefer small, shippable increments over large speculative work

## PRD Alignment Summary
- Phases 1-4 (foundation, guest access, public pages, admin core) are built on the PRD-specified Next.js 16 stack — the Express prototype (`src/server.js`) is now legacy-only, kept for `npm run smoke`/`start:legacy`.
- Admin auth deviates from the PRD's Supabase Auth call-out (env-credential instead — see MEMORY.md 2026-09-03).
- Guest invitation code format deviates from the PRD's `[SURNAME]-[NNN]` spec (see MEMORY.md 2026-09-03 and PRD P0-07).
- Messaging (P1-06/P1-07) has a slim interim slice (single-guest WhatsApp reminder, wa.me link) — the full group-send/template/log spec is still unbuilt.
- Table Planning & Guest Categorization (P1-14) is a newly proposed feature (added 2026-09-03, not in the original PRD scope) — full spec documented in the PRD §14. Built and live (2026-09-04).
- Event manager (P1-09) shipped 2026-09-04 at MVP scope — venue image upload and icon selection (full PRD spec) deferred.
- Pre-Login Site Gate & Invitation Reveal (P1-15) — owner-confirmed 2026-08-29, never carried into this file until rediscovered and logged 2026-09-05 (see Next Action 12, PRD §15). Not started; two open product questions need the user's call before coding.
- Table Arrangement Dashboard & Probable Attendance (P1-16) — newly proposed feature, scoped 2026-09-05 via a Grill Me session; full spec in PRD §16. Built and live 2026-09-05 — migration 010 applied to the live database (HITL-confirmed).
- Still missing from PRD scope: full messaging center, the pre-login gate above, production deploy.

## Model Assignment Convention
- Every open backlog item and Next Action below carries a `Model:` tag — the Claude model recommended for that task's complexity/risk, so it can be confirmed with the user (human-in-the-loop, per `HITL.md`) before work starts.
- Guide: **Opus 5** for architecture-defining, security-sensitive, or safety-critical work (schema/design decisions, anything touching HITL enforcement); **Sonnet 5** for well-scoped feature/CRUD/config work with low ambiguity; **Haiku 4.5** for small, mechanical, single-purpose fixes.
- Completed (`[x]`) items are left untagged.
- If a task is added to this file without a `Model:` tag, flag it and propose one before starting — do not assume a default.

## Implementation Backlog (updated)

### Phase 1 — Foundation
- [x] Set up project structure (skeleton files created)
- [x] Configure minimal `package.json` and test script
- [x] Create initial source module `src/guest-auth/loginGuestByCode.js`
- [x] Configure TypeScript, linting, and testing (basic setup completed)
- [x] Create Supabase schema for core tables (migration files added)
- [x] Add environment configuration placeholder files (`.gitignore`)

### Phase 2 — Guest Access Flow
- [x] Slice 1: Guest can access their invitation with a valid code
  - Status: helper implemented, unit tests present, demo server + smoke test validated locally
- [x] Slice 2: Guest can access their invitation with their name
  - Status: exact name login implemented, ambiguous candidate search returns matching guests, API tests passing
- [x] Slice 3: Guest can recover when their name is ambiguous
  - Status: ambiguous-name candidate selection flow implemented on `/login`, guest can select the correct record and continue to invitation
- [x] Slice 4: Guest can see a personalized invitation and respond
- [x] Slice 5: Guest can change their RSVP later
- [x] Slice 6: Guest sees a clear RSVP reminder until they respond

### Phase 3 — Public Pages
- [x] Home page
- [x] Our Story page
- [x] Celebration page
- [x] Gallery page
- [x] Wishes page
- [x] Shared page wrapper and refreshed wedding-style layout for public pages

### Phase 4 — Admin Experience
- [x] Admin authentication (P0-09) — env-credential login, scrypt hash, signed session cookie with expiry, all `/admin/*` routes protected, logout
- [x] Guest management (P0-07) — add/edit/soft-delete, auto code generation (`[CATEGORY]-[FIRSTNAME]-[random]`, configurable categories via `GUEST_CATEGORIES`), filter by status and group, search by name or code
- [x] RSVP dashboard (P0-08) — invited/accepted/declined/pending counts, individual headcount, breakdown chart, CSV export
- [✔] Event manager (P1-09, MVP scope, 2026-09-04) — `/admin/events` CRUD for celebration events: name, date, time, venue name, venue address (builds the Google Maps link), display order. Replaces the hardcoded Ceremony/Reception array on `the-celebration` and the invitation page's Event Details block. Schema: `celebration_events` (migration 009, applied live). Deferred per user decision: venue image upload and icon selection (full PRD spec) — would need the Supabase Storage adapter (`src/storage/supabaseStorage.js`) ported to the Next.js app, same gap as P1-11/P1-14 had.
- [✔] Messaging center (P1-06/P1-07, 2026-09-05) — `/admin/messages` works through a guest group on WhatsApp one message at a time: pick a template (the four seeded by migration 005), pick an audience (RSVP status + relationship group), then step through recipients — each opens a `wa.me` link with the message pre-filled and records a `message_logs` row, so a later run can skip whoever was already handled ("skip guests who already got this template" is on by default). wa.me is the confirmed permanent approach (no Twilio — MEMORY.md 2026-09-05), so there is no SMS/email channel and no delivery tracking: a log row means *the admin opened WhatsApp*, not that the guest received anything. **No new migration was needed** — `message_templates` and `message_logs` were already created by migration 005 and already applied live, so this slice had no HITL gate. The single-guest reminder button on the guest list (2026-09-03) is unchanged and still works. Deferred: editing template bodies from the admin UI (edits are per-send only, not persisted back to `message_templates`), retry/failed-status handling (nothing can fail — there is no programmatic send), and P1-08 auto thank-you (not achievable via wa.me — see the PRD note).
- [x] Theme editor (P1-10, reconciled 2026-09-04) — `/admin/theme` edits primary/secondary/accent colors and font family/style, applied site-wide instantly via CSS custom properties set on `<html>` in `app/layout.tsx`. Backend is the richer schema built on `feature/nextjs-supabase-migration` (`src/theme/themeRepo.js`, `theme_settings` from migrations 003–004, already applied to the live DB — also holds palette/font-choice/hero-image/invitation-overlay/couple-venue columns, not yet exposed in this simple form). Font choice is a curated set (Default, Cormorant Garamond, Playfair Display, Cinzel) self-hosted via `@font-face` (`src/theme/fontFaces.js` + `public/fonts/`), not free text or Google Fonts — an arbitrary name would silently fail to render, and self-hosting avoids a Google Fonts dependency. `main` had independently built a competing slim implementation in parallel (see MEMORY.md 2026-09-04 "Past mistakes"); its working `/admin/theme` page, `ThemeEditor` component, and API route were kept and rewired onto the richer schema, its competing migration/backend dropped, and both merged into `main` together via PR #4. Still deferred: hero/invitation image fields, invitation name-overlay rendering, `couple_names`/`wedding_date`/`venue_name`/`venue_address` (columns exist, not wired into any render path — see Next Action 1a), `patterns`/`custom_css` (no rendering consumer), and the palette/font-choice pickers (`src/theme/palettes.js`) themselves have no admin UI yet.
- [✔] Section manager (P1-11) — backend (`src/sections/sectionsRepo.js`, `validateSection.js`) merged from feature/ui-wrapping 2026-09-04; admin UI wired into the live Next.js app 2026-09-04 (`app/admin/sections`, `app/api/admin/sections`, `SectionManager.tsx`) after a docs audit found it backend-only. Visible sections now render on all five public pages via `components/public/CustomSections.tsx`.
- [✔ code, ⏳ live] Image uploads for Sections and Event venue photos (2026-09-11) — owner asked for real upload buttons after finding the "image" section type did nothing. Built on **Vercel Blob** (owner's choice over standing up Supabase Storage, which this project has never actually configured — confirmed `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` empty in the real `.env` — or a paste-a-link workaround). New: `app/api/admin/upload/route.ts` (admin-auth + CSRF, same pattern as every other admin route), `src/storage/blobStorage.js` (size/MIME/magic-byte validation — mirrors the magic-byte check Action 8 already established was needed, since `Content-Type` alone is spoofable — plus the actual `@vercel/blob` `put()` call), `components/admin/ImageUploadField.tsx` (shared upload widget used in both Section and Event admin forms/rows). `image_url` added to `site_sections` and `celebration_events` via migration 011 — **applied to the live database 2026-09-11**, owner-confirmed HITL go-ahead (see Next Action 20). Public rendering: `CustomSections.tsx` and `the-celebration/page.tsx` show the photo via a plain `<img>` (not `next/image`, to avoid adding Vercel Blob's domain to `next.config.ts`). No cleanup of replaced/deleted images from Blob storage — deliberate simplification for a low-volume personal site. Verified: 388/388 tests (13 new, including a spoofed-Content-Type case mirroring Action 8's), clean build/lint/tsc, and a live check of the auth/CSRF/graceful-error paths via curl against a throwaway local server (401 unauthenticated, 503 `storage_not_configured` with a real PNG once authenticated — there is no `BLOB_READ_WRITE_TOKEN` yet, so a real upload has not been exercised). **Not yet usable in production** — still needs Next Action 20(a) (Vercel Blob storage enabled) below; the schema half, 20(b), is done.
- [✔] Table planning & seating management (P1-14) — backend (`src/table-arrangement/tableArrangementRepo.js`, `tableArrangementExport.js`) merged from feature/ui-wrapping 2026-09-04, schema `participants`/`seating_tables` (migrations 007–008); admin UI wired into the live Next.js app 2026-09-04 (`app/admin/table-arrangement`, `app/api/admin/table-arrangement`, `TableArrangement.tsx`) after the same docs audit found it backend-only.
- [✔] Table Arrangement Dashboard & Probable Attendance (P1-16, 2026-09-05) — RSVP-funnel stats (Accepted / Table Arranged / BalanceToArrange / RSVP Not Accepted) on `/admin/table-arrangement`, plus an Admin-editable "probable attendance" buffer per Declined/Pending bucket that materializes anonymous `ProbableAttendee` seat placeholders (no name, no link to a real Guest, `rsvp_status` never touched — see PRD §16). Schema: `probable_attendees` + `table_seats.probable_attendee_id` (migration 010, HITL-confirmed and applied to the live database 2026-09-05). Built in both the Next.js app (`app/api/admin/table-arrangement/*`) and the legacy Express server (`src/server.js`), since the existing test suite drives the latter. Verified: 311/311 tests (19 new), clean build/lint, and a full live browser click-through (buffer resize, seat/unseat a placeholder, shrink-below-seated rejection, export content) — first against a throwaway in-memory server, then again against the real database after the migration landed. Model used: Sonnet 5.

### Phase 5 — Polish & Launch
- [✔] Mobile responsiveness review (2026-09-06) — public pages and all seven admin pages walked at narrow width. One blocking defect found and fixed: `AdminNav` links were unreachable below ~600px (`da0cd9d`); one theming bug fixed on the login page. Detail in Next Action 9. **Not proven at a true 375px** — the Browser pane would not honour viewport emulation, so elements were measured against a 375px budget and checked for a scrollable ancestor instead. Worth 30 seconds on a real handset before launch.
- [ ] Content fill-in and final copy — Model: Sonnet 5
- [ ] Deployment to Vercel (HITL required) — Model: Sonnet 5
- [ ] Final QA and launch readiness — Model: Opus 5

## Current Slice Details

### Slice 1: Guest can access their invitation with a valid code
- User value: A guest can enter a valid invitation code and reach their personalized invitation page.
- Acceptance criteria:
  - Valid code logs the guest in and creates a session
  - Invalid code shows a friendly error and does not create a session
  - Soft-deleted guests cannot log in
  - Unit tests covering code-paths run green locally

## Completed So Far
- Created `package.json` with test script
- Created `.gitignore`
- Implemented `src/guest-auth/loginGuestByCode.js`
- Added unit test `tests/guest-login.test.mjs`
 - Implemented `src/guest-auth/loginGuestByCode.js`
 - Implemented `src/guest-auth/loginGuestByName.js` (name lookup helper)
 - Added unit tests: `tests/guest-login.test.mjs`, `tests/guest-login-name.test.mjs`
 - Unit tests passed locally for code-based login; name-based tests scaffold added and helper implemented
 - Added demo Express server (`src/server.js`) and smoke test (`src/smoke.js`) that exercise login + invitation page
- Implemented RSVP submission flow, sticky reminder bar, and change-response logic on `/invitation/:code`
 - Pinning/adjusting linting and TypeScript devDependencies to compatible versions (ESLint v8 + TS v5)

## Admin Slice — Follow-ups

- Admin login throttling is in-process only; move the counter to a shared store before running more than one instance.
- `guests` has no `updated_at` column, so the guest list shows RSVP status rather than the "last updated" timestamp the PRD wording suggests. Adding the column needs a migration (HITL).
- `ADMIN_PASSWORD_HASH` is verified with Node scrypt. A hash generated by a different KDF will not match — regenerate with `npm run admin:set-password`.
- Admin auth is env-credential based rather than Supabase Auth (the mechanism named in the PRD), because `ADMIN_EMAIL` and `ADMIN_PASSWORD_HASH` were already provisioned in `.env`. "Forgot password" therefore has no email flow yet.
- `npm test` now runs every file in `tests/`. The legacy `guest-login-api` tests had been failing since CSRF was added to the demo server because their helper never sent a token; the helper now fetches one.
- (2026-09-04) `tests/guest-login-api.test.mjs`'s RSVP test was stale — it predated a guest-session requirement added to `/api/guest/rsvp` and got a 401 instead of 200; fixed by logging in first. All 5 tests in that file were also switched to a `withApp()` try/finally helper, since the failing assertion had been leaking its HTTP server and hanging the *entire* `npm test` run, not just that file. See MEMORY.md 2026-09-04.
- Public routes moved into an `app/(public)/` route group so `/admin` no longer inherits the wedding header and footer. URLs are unchanged.

## Code Format & Messaging Follow-ups (2026-09-03)

- The 4-5 guests created during earlier manual testing (Ruwan, Sunil, Kamala, Nimal, Thar) still carry the old `[SURNAME]-[NNN]` code. They still work (code isn't re-validated against the new format), but any admin edit does not retroactively regenerate the code — only new guests get the new format. Regenerating the old ones means deleting and re-adding them (their RSVP history would need to be preserved separately, or accepted as a fresh start).
- `GUEST_CATEGORIES` is read from `process.env` on every call (`src/admin/categories.js`) — no restart-free hot reload guarantee outside Next.js dev's own env-var handling; confirm behavior before relying on changing it without a redeploy in production.
- WhatsApp reminder button only renders for guests with `rsvpStatus === 'pending'` AND a `whatsappNumber` on file. No UI path yet for guests without a WhatsApp number (accepted/declined guests, or pending guests with no number).
- No message send is logged anywhere (no `MessageLog` — PRD P1-07 domain concept still unimplemented). If audit history of reminders sent becomes a requirement, this needs a data store, which is a schema decision (HITL-gated migration).
- `NEXT_PUBLIC_SITE_URL` is not set in `.env`; the admin guest page falls back to `http://localhost:3010` for the invitation link inside reminder messages. Must be set correctly before this feature is usable against a deployed site.

## Current Blockers
- Supabase integration is partially wired: migration runner and local seed script exist, but a live database connection still requires `DATABASE_URL`.
- Session handling now uses signed cookies, but further production hardening is still advisable before any public exposure.

## Next Actions (step-by-step)
1. ~~Theme editor (P1-10)~~ — done, reconciled 2026-09-04 (see Phase 4 note above).
1a. ~~Wire `theme_settings.couple_names`/`wedding_date` into public pages~~ — done 2026-09-04. `SiteHeader.tsx`, `PageFooter.tsx`, `Countdown.tsx`, `app/layout.tsx` metadata, all five public page `<title>`s, and the invitation page's sticky RSVP bar now render live `theme_settings` values instead of hardcoded "Amandi & Tharindu" / 14 Dec 2026. New `src/theme/formatWeddingDate.js` shared helper. Verified via `npm test` (256/256), `npm run build`, and a live browser walkthrough against the DB. Countdown's ceremony start time (15:00) stays hardcoded — no time-of-day column exists on `wedding_date`. The invitation page's Ceremony/Reception venue/time details were deliberately left alone — `venue_name`/`venue_address` wiring belongs to Event Manager (P1-09, Next Action 3), not this slice.
2. ~~Section manager (P1-11)~~ — done, merged from feature/ui-wrapping 2026-09-04 (see Phase 4 note above).
3. ~~Event manager (P1-09)~~ — done 2026-09-04, MVP scope (see Phase 4 note above). Venue image upload + icon selection deferred as a follow-up if the full PRD spec is wanted later.
4. ~~Full messaging center (P1-06/P1-07)~~ — done 2026-09-05, Model: Sonnet 5 (confirmed). Built on the wa.me approach per the same-day no-Twilio decision. New: `src/messaging/` (`messageTemplatesRepo.js`, `messageLogRepo.js`, `selectRecipients.js`), in-memory fallbacks in `src/data/`, `app/api/admin/messages` + `.../messages/log`, `app/admin/messages` + `MessagingCenter.tsx`, and a Messages link in `AdminNav`. `renderTemplate()` was extended to accept the PRD's `[Name]` placeholder spelling alongside the existing `{name}` one, plus `[Date]`/`[Venue]`. **The `MessageLog` migration this action called for turned out to be unnecessary** — migration 005 had already created `message_templates` (with all four templates seeded) and `message_logs`, both already applied to the live DB, so nothing was migrated and no HITL gate applied. Verified: 292/292 tests (26 new), clean build/lint, live read-only DB check of every repo path plus a rolled-back INSERT proving the log statement matches the real schema, and HTTP checks confirming the page redirects (307) and both APIs 401 when unauthenticated. The logged-in click-through was not tested — that needs the admin password, which was not requested. One bug the DB check caught that tests could not: the in-memory template seeds mirrored migration 005's original text and so still carried the pre-migration-008 couple-name order ("Amandi & Tharindu"); fixed, with a regression test.
5. ~~Table planning & seating management (P1-14)~~ — done, merged from feature/ui-wrapping 2026-09-04 (see Phase 4 note above).
6. Regenerate the old-format guest codes (Ruwan, Sunil, Kamala, Nimal, Thar) created during testing, or accept them as-is (they still function). (Model: Haiku 4.5)
7. Enforce HITL programmatically: add a reusable preflight helper (CLI and CI check) that prints the exact `HITL.md` prompt and requires explicit confirmation before deploys, migrations, sending messages, secrets changes, or pushes to production. **Partly done — found 2026-09-10, not built this session.** This was listed as fully unstarted, but `scripts/hitl-preflight.js` and `npm run hitl:migrate` already exist and work: the helper prints the checkpoint text and refuses to run unless `--yes` is passed or `HITL_CONFIRM=yes` is set. It covers **`migrate` only** — its `prompts`/`commands` maps each have exactly one key. Still missing: the other four action types named above (deploys, sending messages, secrets changes, pushes to production), and the **CI check half — nothing enforces any of this in CI today**. (Model: Opus 5)
8. Security & production readiness — **partly done 2026-09-06** (Opus 5). **Security work closed 2026-09-09:** PR #7 (`claude/security-production-readiness-h3cvo7` — an alternative rate limiter, security headers, a spoof-resistant client-IP resolver and a sanitizing security logger) was **closed unmerged**, superseded by the rate limiting and headers `main` built independently on 2026-09-06. Merging it would have left two rate limiters in the repo. Dropped with it: `TRUSTED_PROXY_COUNT` (never landed — do not add it to any deployment doc) and the security logger. The branch is kept as the only copy of that code and must **not** be deleted. Full reasoning, including why the logger was the weakest item in it, in MEMORY.md 2026-09-09. Shipped: guest login rate limiting (10 attempts / 10 min / IP, in both the Next.js route and the Express prototype so the suite can test it over HTTP — 4 new tests), magic-byte image validation wired into the **legacy Express prototype only** — `src/server.js:1398`, reachable via `npm run start:legacy`, **not** the production Next.js app (2 new tests, driven against `src/server.js` via `createApp()`; multer's `fileFilter` only ever saw the client's own Content-Type, so a renamed executable claiming `image/png` used to pass). **Wording corrected 2026-09-10** — this previously read "actually wired into `/api/admin/upload`", implying a production route that has never existed: `app/api/` contains 21 routes and none of them is an upload route. Nothing is exposed today, because production serves no upload endpoint at all — but if image upload is ever ported to `app/`, this validation must be ported with it. See MEMORY.md 2026-09-10, and security headers via `proxy.ts`. Three corrections to the earlier attempt at this: the file was written as `middleware.ts`, which Next.js 16 **deprecated in favour of `proxy.ts`**; its CSP had no `unsafe-eval` in development, which silently broke React's dev overlay and stack traces; and it shipped `X-XSS-Protection`, now removed — the legacy XSS auditor is deprecated and was itself a vulnerability source, CSP replaces it. Verified live over HTTP, not just asserted: headers present on a real response, and the 11th login attempt actually returns 429. **Right-sized 2026-09-06** against `docs/VIBE_CODING_PRODUCTION_CHECKLIST.md`'s own project-tier table (pending merge on PR #6; a small/personal project like this one is scoped to **Tier 1 only**) — see MEMORY.md 2026-09-06 for the full reasoning. **Dropped as Tier 3** (the checklist's own worked example for this project names this exact item as prior over-engineering): moving rate-limit counters to a shared store. **Dropped as already satisfied at Tier 1:** CSRF token TTL — the existing double-submit-cookie protection is the Tier 1 bar; a TTL is a Tier 2+ refinement, not a gap. **Moved off this item, not a security control:** `updated_at` on `guests` is a data-quality nicety needing its own HITL migration decision — revisit only if requested; `NEXT_PUBLIC_SITE_URL` moved to Action 10 (deployment), where it belongs as a prod env var. **Kept, still open (Tier 1):** a guest-data privacy review (no personal data in logs; confirm the existing soft-delete satisfies "a way to delete a person's data on request"). Reusable pieces found 2026-09-04 in archived branch `archive/claude/mobile-only-session-qj1ixz-2026-09-04` (never merged, and no longer relevant now that shared-store rate limiting is dropped): rate-limiting middleware (`src/rate-limiter.js`, tested) and security-headers middleware (`src/security-headers.js`) — the request-logging/sanitization middleware (`src/request-logger.js`) there is also not being pursued. That branch's RLS-policy migration and its own admin-auth scaffolding remain NOT reusable as-is — see MEMORY.md 2026-09-04 "Abandoned Branches" for why. (Model: Sonnet 5 for this right-sizing pass; Opus 5 for the original hardening work above)
9. Mobile responsiveness review across public + admin surfaces. (Model: Sonnet 5) — **public pages reviewed 2026-09-06** at 375×812: home, login, the-celebration, gallery all render without horizontal scroll or clipping. Two findings, one fixed: the login page hardcoded Tailwind blues (`bg-blue-600`, `bg-sky-100`) instead of the theme variables every other page uses, so the Theme Editor did not reach the guest entry page — fixed by moving its buttons onto the shared `.button`/`.button-primary` classes. Still off-palette and left alone deliberately: the success (green) and candidate-list (blue-50) panels on that page, which are transient semantic states. **Admin surfaces reviewed the same day** (owner signed in to the live-DB dev server themselves; no admin data was modified, and the session was logged out afterwards). One blocking defect found and fixed: `AdminNav`'s `<nav>` was a non-wrapping flex row with `overflow-x: visible`, so below roughly 600px the last links ran past the right edge **and the nav did not scroll** — Events, Sections and Table Arrangement were simply unreachable on a phone. Fixed with `flex-wrap`, matching the public nav; all seven links now reachable on two rows. All seven admin pages otherwise clean: no page-level horizontal overflow, and the one genuinely wide element (the 666px guest table) already sits in an `overflow-x: auto` parent. Caveat on method: the Browser pane would not honour viewport emulation (`window.innerWidth` stayed at the pane's own ~578px), so rather than claim a 375px test that did not happen, elements were measured against a 375px budget and checked for a scrollable ancestor. Untested at a true 375px: nothing is expected to break, but it has not been proven.
10. ~~Deployment prep: Vercel config, production env vars, HITL-gated deploy~~ — **done 2026-09-10** (Model: Sonnet 5, confirmed). Live at **https://amandi-tharindu-website.vercel.app** (Vercel project `rangajeewa-s-projects/amandi-tharindu-website`, GitHub-connected to `main`).

    **Owner decision recorded first:** the Neon password reset (Action 18a) was deferred by the owner rather than done before this deploy, so the current, already-exposed-in-chat database password is what `DATABASE_URL` carries in Vercel today. The scratch-branch deletion (18b) was confirmed done. This is now the project's top open item — see Action 18a.

    **What a session cannot do without your Vercel account:** logging into Vercel needed a device-code approval in your browser (the CLI's `vercel login`, not the web dashboard — the dashboard UI proved too hard to navigate blind, see the mistake below), and the `vercel deploy --prod` command itself was blocked by this environment's own safety classifier both times, so you ran it yourself from a command I gave you, both for the first deploy and the redeploy after setting `NEXT_PUBLIC_SITE_URL`.

    **Vercel CLI setup, for the next session that touches deployment:** installed globally (`npm install -g vercel`). Auth and project state live in a fixed folder, `C:\Users\User\.vercel-cli-config`, passed explicitly via `--global-config` on every command — **do not omit this flag**, see the mistake below for why. The project name had to be `amandi-tharindu-website` (not the repo's own `Amandi-Tharindu---Website`): Vercel rejects a `---` triple-dash sequence in project names.

    **Env vars set in Production:** `SESSION_SECRET` (freshly generated, see Action 18c), `DATABASE_URL` (current value, see the owner decision above), `ADMIN_EMAIL`, `ADMIN_PASSWORD_HASH` (both copied from local `.env`), `NEXT_PUBLIC_SITE_URL` (set after the first deploy revealed the assigned domain, then redeployed to bake it in — same two-step order `docs/DEPLOYMENT.md` describes).

    **Verified from this session, without needing guest or admin credentials:** couple names/date correct on the gate; all seven previously-public routes plus `/login` show only the gate with no real content in the HTML; wrong code shows the inline error with no reload; `/admin` is not gated and shows the sign-in form; all four security headers present (CSP, HSTS, X-Frame-Options, X-Content-Type-Options); no horizontal overflow at a real 375px width (confirmed by `getBoundingClientRect`/`scrollWidth`, not just a screenshot — see the mistake below about why that distinction mattered here).

    **Found and documented, not a bug to fix:** Vercel's own automatic traffic mitigation (`vercel firewall status` confirms `Attack Mode: Off`, `Bot Protection: Off` — this is a separate, automatic layer) briefly showed a "Vercel Security Checkpoint" / "We're verifying your browser" page during this session's own rapid automated testing, clearing itself within seconds once that testing paused. Real guests browsing normally over hours are very unlikely to trigger it. Noted in `docs/DEPLOYMENT.md` so a future session doesn't mistake it for an app bug.

    **Still needs the owner, because it needs real credentials §5 of `docs/DEPLOYMENT.md`:** log in with a real invitation code and submit an RSVP, confirm it persists (proves `DATABASE_URL` is really live, not in-memory); log in to `/admin` and confirm the real guest list appears; open a WhatsApp reminder in `/admin/guests` and confirm the link now starts with the real domain, not `localhost`.
    - `docs/DEPLOYMENT.md` covers every environment variable (what it does, where its value comes from), the deploy steps, six post-deploy checks, and rollback. Derived from the code with `file:line` references so each claim is checkable — but **nothing in it has been executed against a real Vercel project**, and the file says so at the top.
    - **Why `NEXT_PUBLIC_SITE_URL` is the one that matters:** `app/admin/guests/page.tsx:24` and `app/admin/messages/page.tsx:60` fall back to a hardcoded `http://localhost:3010`, and that value flows through `buildInvitationLink()` into the WhatsApp text sent to guests. Deploying without it set would send **every guest a dead link**, with nothing erroring and the admin panel looking normal. No code change was made — the fallback is right for local development; the runbook makes setting the variable step §1, with a check to run before messaging anyone.
    - No `vercel.json` is needed; Vercel detects Next.js automatically. Migrations are not applied by the deploy — `npm run migrate` stays manual and HITL-gated.
    - [ ] Remaining: the deploy itself, which needs the owner's Vercel account. Note `docs/DEPLOYMENT.md` §8 ranks the Action 14 restore rehearsal **above** this, since losing the guest list is the only failure here that cannot be undone. (Model: Sonnet 5)
11. ~~Wire Section Manager (P1-11) and Table Planning (P1-14) admin UI into the live Next.js app~~ — done 2026-09-04, Model: Sonnet 5 (confirmed — same model as this session). `app/admin/sections` + `app/api/admin/sections`, `app/admin/table-arrangement` + `app/api/admin/table-arrangement`, both consuming the already-tested `src/sections/*`/`src/table-arrangement/*` repos with no backend changes. Verified via `npm test` (256/256), `npm run build`, `npm run lint`, and a live check of both pages/APIs' auth guards against the running dev server (redirect/401 as expected). The logged-in CRUD flows were not click-tested — that needs the admin password, which wasn't requested from the user. See Phase 4 notes above for detail.
12. ~~Pre-Login Site Gate & Invitation Reveal (P1-15)~~ — **done 2026-09-10: built on branch `feature/p1-15-site-gate`, merged to `main` via PR #11 (merge commit 30c50d8) after the owner's HITL approval; branch deleted. Not yet deployed** (Model: Opus 5, confirmed). The owner answered every open question in a Grill Me session recorded in PRD §15 (2026-09-10): lock everything except the couple's names, behind a blurred look-alike of the site drifting in the theme's colours; remove name login; the WhatsApp link opens the gate and the guest types the code; keep the envelope reveal. **Built:** `proxy.ts` rewrites every signed-out guest-facing path to `app/gate/page.tsx` (rules in `src/site-gate.js`); `components/public/SiteGate.tsx` + `EnvelopeReveal.tsx`, styled in `app/globals.css`; `/api/guest/login` is code-only and case-insensitive; the sign-in cookie now lasts `GUEST_SESSION_TTL_DAYS` (30) instead of ending with the browser; `/login` redirects; a new `/invitation` sends a guest to their own card; a guest opening another guest's `/invitation/CODE` is sent to their own. **Security hole found and closed on the way:** `/api/guest/rsvp` never checked the session — anyone could overwrite any guest's RSVP, and its unlimited 404-vs-200 answers let codes be tested around the login rate limit. It now only accepts the signed-in guest (`src/guest-auth/authorizeRsvp.js`). **Verified:** 375/375 tests (10 new in `tests/site-gate.test.mjs`), clean build, lint at the 4 pre-existing problems; in the browser against an in-memory server — raw HTML and RSC payload of all 8 gated routes contain the gate and none of the hidden content, `/admin` ungated, wrong code shows the inline error, lowercase code opens the reveal then the invitation, cross-guest RSVP returns 403, cookie `Max-Age=2592000`, true 375px phone viewport with no horizontal overflow. **Not verified:** pressing Enter to submit (the browser tool's synthetic key did not submit even a control form, so this is unproven either way — the form is a standard submit form with no key handling of its own); the reduced-motion path (no way to emulate it here). All 6 live guest codes were confirmed already upper case (read-only count), so case-insensitive matching locks nobody out. **Before sending `reminder_2` to anyone**, add `[Code]` to it — see PRD §15 consequences. Original scoping note follows. — scoped 2026-09-05. Owner-confirmed 2026-08-29 (`docs/WEDDING_UI_UX_SPEC.md` §4/§6, `docs/MEMORY.md` same date) but never carried into this file at the time, so it was never built — rediscovered when the user asked whether the site had a "locked until code entered" gate. Full spec, requirement breakdown, and open questions in PRD §15. Two things must be resolved with the user *before* coding starts, not assumed: (a) whether gating every public page behind a code is still wanted given it makes them unindexable/unshareable — the site may have been deliberately shipped ungated as a later call, not an oversight; (b) whether this replaces or coexists with today's working name-based login (Phase 2, Slices 2–3). Touches site-wide routing (a `proxy.ts`-level gate across the whole `app/(public)/` route group — note `proxy.ts` now exists and sets security headers, so the gate would extend it rather than create it; `middleware.ts` is deprecated in Next.js 16, see MEMORY.md 2026-09-06) plus a real animation sequence (envelope open, ~2s) — an architecture decision, not just a UI slice. (Model: Opus 5 — proposed, not yet confirmed per the Model Assignment Convention; the routing/auth-interaction decision is the Opus-level part, the animation polish afterward could plausibly hand off to Sonnet 5 once the mechanism is settled)
13. ~~Table Arrangement Dashboard & Probable Attendance (P1-16)~~ — done 2026-09-05, Model: Sonnet 5 (confirmed). See Phase 4 note above for full detail. Open questions from the original scoping were resolved during implementation: placeholder labels are "Probable (Declined/Pending) #N"; ProbableAttendee resizing has no admin-role restriction yet (not GuestPartition-scoped, and the project has no live two-admin model to gate it against — see the `admin_users.role` prototype-deviation note in `WEDDING_DATABASE_SCHEMA.md`); headline stats confirmed strictly Accepted-Guest-only, per PRD §16.
14. ~~Backups~~ — done 2026-09-10 (Model: Opus 5 for original build, Haiku 4.5 for 2026-09-10 rehearsal). Application-level backup shipped and run: `npm run backup` writes every table to `backups/*.json` (tables discovered from the catalog, so a future migration cannot silently leave its table unbacked), `npm run backup:verify` reads the newest one back. `backups/` is gitignored — the files contain every guest's name, phone and email, so they must never be committed or put in a shared folder. First real backup taken against the live database (36 rows / 12 tables). **Restore path built 2026-09-06** (Opus 5): `scripts/restore-db.js` + `src/restore-plan.js`, 16 new tests (354/354 total, lint clean). Insert order is derived from the target database's own foreign keys rather than a hardcoded list — the doc previously carried that list in prose, which is exactly the drift risk the backup script avoids by reading the catalog; sequences are reset after the rows land, so the next insert cannot collide with a restored id; the whole load runs in one transaction and rolls back as a unit. The target is named by `RESTORE_TARGET_URL` and **never** falls back to `DATABASE_URL`, so a loaded `.env` cannot reach production — this guard was **observed refusing twice against the real live URL**, not asserted. `npm run restore:dry-run` connects, plans and writes nothing. **Correction to this item's earlier framing:** it said the rehearsal was not automated because it needed a scratch database. That was not the real blocker — there was no restore code at all, so the rehearsal was impossible for anyone, owner included; the scratch database was only ever the second obstacle. **Off-machine storage decided 2026-09-06** (owner): encrypt the backup, then store it anywhere private. `npm run backup:encrypt` / `npm run backup:decrypt` (`scripts/backup-crypto.js` + `src/backup-crypto.js`, 11 tests) use AES-256-GCM with a scrypt-derived key from `BACKUP_PASSPHRASE` — Node crypto rather than `age`/`gpg`, on the same reasoning that ruled out `pg_dump`. The encrypt step decrypts its own output and compares byte-for-byte before reporting success, and only then will `--remove-plaintext` delete the original. Verified end-to-end on synthetic data (encrypt, refuse-to-overwrite guard, decrypt, checksum match). **Completed 2026-09-10:** restore rehearsal into scratch Neon branch, BACKUP_PASSPHRASE set and stored in password manager, `.enc` file encrypted and moved to Google Drive. **Result: "we have backups" is proven — a real restore of 36 rows across 12 tables succeeded into a real Postgres (Neon scratch branch), and every table matches the backup.** One remaining task only: (a) confirm Neon's point-in-time retention window (read off the Neon console). The restore path was the highest-priority open item; it is now complete and verified. (See `docs/BACKUP_RECOVERY.md` for the full record.) **Correction (2026-09-10, later session):** the scratch branch was created from the live database, so it already held the schema and a copy of the data; `npm run migrate` reported "already up to date" rather than applying anything, and the restore ran with `--replace`, clearing the copied rows first. The rehearsal therefore proves rows come back into a real Postgres, but not a restore onto an *empty* database built from `migrations/` alone. Also recorded: the restored backup was the 2026-09-06 one; no fresh backup was taken that day. The Neon retention window (7 days) was confirmed the same day.
15. Doc/tooling drift found 2026-09-06, filed rather than fixed (all three are code or deletion changes, out of scope for a docs pass): (a) ~~**`npm run lint` is effectively unusable**~~ — **resolved 2026-09-10.** It reported 5,298 problems, of which roughly 5,290 came from the leftover `.claude/worktrees/focused-kilby-265cc0` folder; only about eight were in real source files. This is why a previous session could report "lint clean" while 383 errors existed. Fixed by **deleting the merged worktree** (`git worktree remove --force`), not by an eslint ignore: `npm run lint` now reports **4 problems** — 2 errors in `bin/append-memory.js` (`require()` imports) and 2 warnings in `scripts/ci-check-docs.js` (unused `err`/`err2`), all pre-existing and unrelated. The eslint ignore is still worth adding so the next worktree cannot reintroduce this — filed as Next Action 17. **(b), (c) and (d) resolved 2026-09-10** (commit ddb4eaa): `.env.example`'s `NEXT_PUBLIC_SITE_URL` comment now says what the variable really does, the Twilio entries are gone, and `SECURITY_HARDENING_SUMMARY.md` was deleted. That deletion did not fold its content in first as (c) asked; its one point not recorded elsewhere — RSVP saving is two separate writes, not one transaction — is now Action 19(d). Original notes: (b) **`.env.example` describes behaviour that does not exist** — it calls `NEXT_PUBLIC_SITE_URL` the "origin used for CSRF/Origin verification in proxy.ts", but that variable is read only by `app/admin/guests/page.tsx` and `app/admin/messages/page.tsx` to build WhatsApp invitation links, and no origin verification exists anywhere (`src/csrf.js` is a pure double-submit cookie compare that never inspects origin). (c) **`SECURITY_HARDENING_SUMMARY.md` sits at the repo root**, off-convention and overlapping this file; candidate to fold into TASKS.md/MEMORY.md and delete. (d) **`.env.example` still lists `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN`/`TWILIO_WHATSAPP_FROM`** under "Deferred integrations" (found 2026-09-06, second session), contradicting the 2026-09-05 decision that Twilio is *permanently* off the stack — that cleanup pass covered the PRD, AGENTS.md, TASKS.md, README.md and HITL.md but missed `.env.example`. Keep `RESEND_API_KEY`; email via Resend is unaffected. Historical MEMORY.md entries must be left alone (append-only). (Model: Haiku 4.5 — mechanical, but (b) is a wording decision the owner may want to make)

16. Branch cleanup, owner-only (added 2026-09-09). `delete_branch_on_merge` is now **enabled** on the repository, so branches merged from here on clean themselves up. It does not apply retroactively, so four already-merged branches still need deleting by hand once: `claude/vibe-coding-checklist-markdown-h3cvo7`, `claude/mobile-admin-nav-fixes-h3cvo7`, `claude/deployment-runbook-h3cvo7`, and `feature/ui-wrapping` (the last was recorded as deleted on 2026-09-04 but is still present — that attempt evidently hit the same wall). **Do not delete `claude/security-production-readiness-h3cvo7`** — it is unmerged and the only copy of closed PR #7's code. This is owner-only because the sandbox proxy refuses both `git push --delete` and the REST ref-delete with "Write access to this GitHub API path is not permitted through this proxy"; repository-settings writes are refused the same way, so the auto-delete setting above was enabled by the owner, not by a session. Two clicks each at https://github.com/jkdtharindu/Amandi-Tharindu---Website/branches. (Model: Haiku 4.5 — but no model can do it from a session; it needs a browser or a developer machine)

17. ~~Add an eslint ignore for `.claude/`~~ — **done 2026-09-10** (commit d94ddb1; `.claude/**` in `eslint.config.mjs`'s `globalIgnores`). The `SESSION_END_PROMPT.md` Step 4 simplification mentioned below is still not done. Original note: (added 2026-09-10). Next Action 15(a) was resolved by deleting the merged `focused-kilby-265cc0` worktree, which dropped `npm run lint` from 5,298 problems to 4 — but nothing stops the next Claude Code worktree from reintroducing exactly the same noise, and the failure mode is quiet rather than loud: lint output becomes too long to read, so a session reports "lint clean" without ever having seen its own errors. That has already happened once here (383 errors missed, see MEMORY.md 2026-09-06). The fix is one entry in `eslint.config.mjs`'s `globalIgnores([...])`. Related follow-up once it lands: `SESSION_END_PROMPT.md`'s Step 4 checklist currently instructs sessions to expect worktree noise and report only changed-file problems — with the ignore in place, that instruction can be simplified to trust the raw total. (Model: Haiku 4.5 — proposed, unconfirmed)

18. Owner-only security clean-up after the 2026-09-10 restore rehearsal (added 2026-09-10). Each touches secrets or deletes data, so each is HITL. (a) **Reset the `neondb_owner` password** in the Neon console, then update `DATABASE_URL` in `.env` and in Vercel Production. **Owner decision 2026-09-10: still deferred.** The scratch-branch connection string pasted into that session's chat carried the same password as the live database (Neon branches inherit the parent's role passwords), so the live password is in a chat transcript — and as of the same-day deploy (Action 10), that password is now also set as `DATABASE_URL` in Vercel Production, i.e. live. This is the one remaining item from this list with real exposure, since the site itself is now reachable from the internet. **Still the owner's call, but no longer a "nothing is deployed yet" situation** — reset it when convenient, then update `DATABASE_URL` in both `.env` and Vercel and redeploy. (b) ~~**Delete the scratch Neon branch**~~ (`ep-crimson-lab-…`) — **done 2026-09-10** (owner-confirmed). (c) ~~**Use a new `SESSION_SECRET` in production**~~ — **done 2026-09-10**: generated and set as the `SESSION_SECRET` Vercel Production env var during the Action 10 deploy. (Model: none — owner, in the Neon console)

19. Small code follow-ups found while building the site gate (added 2026-09-10, not started). (a) **`/api/csrf` issues a new token on every call** and overwrites the cookie, unlike the Express prototype's `getOrCreateCsrfToken`, which reuses a valid one. Opening the site in a second tab therefore silently breaks the first tab's forms. The gate works around it by retrying once; **the RSVP form (`InvitationClient.tsx`) does not**, so a guest with two tabs open gets an error on submit. Fix at the source: reuse a valid existing cookie. (b) **No guest sign-out.** With the 30-day sign-in, a guest on a shared device cannot sign out. (c) The sign-in cookie's value (the guest id plus a signature) never expires on the server side; only the browser's `Max-Age` ends it. Fine at Tier 1; note it if the threat model changes. (d) RSVP saving is two separate writes (`upsertRsvpResponse`, then `updateGuestRsvpStatus`), not one transaction — a failure between them leaves the status stale, which the route logs rather than fails on. (Model: Sonnet 5 — proposed, unconfirmed)

20. Owner-only setup to make the 2026-09-11 image-upload feature (Phase 4 above) actually usable, added 2026-09-11. (a) **Enable Vercel Blob storage** for this project in the Vercel dashboard (Storage → Create Database → Blob) — this provisions `BLOB_READ_WRITE_TOKEN` automatically as a Production env var; without it the upload button returns a clear "not configured" error rather than working. For local testing, pull the token down with `vercel env pull .env.local` or copy it from the dashboard into `.env` by hand. **Still open.** (b) ~~**Apply migration 011**~~ — **done 2026-09-11**, owner-confirmed HITL go-ahead. `npm run hitl:migrate -- --yes` applied `migrations/011_add_image_url.sql` to the live database; verified by a direct read-only query, not just the script's own output: `site_sections.image_url` and `celebration_events.image_url` both exist, and `schema_migrations` lists all 11 files matching `migrations/*.sql` exactly, no drift. Until (a) is done, the upload UI is visible but inert. (Model: none — (a) needs the Vercel dashboard)

## Notes
- Mark tasks as done only after tests are written, run, and confirmed green.
- Use `HITL.md` for any actions that require explicit human approval (deploys, migrations, pushes to main, sending messages).
- Keep this file updated after each completed slice.
- Tag every new backlog item / Next Action with a `Model:` recommendation (see Model Assignment Convention above) and confirm it with the user before starting work on it.
